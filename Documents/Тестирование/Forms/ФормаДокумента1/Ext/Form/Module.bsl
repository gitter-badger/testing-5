
&НаКлиенте
Процедура ПриОткрытии(Отказ)
	Если Объект.Ссылка.Пустая() Тогда
		Отказ = Истина;
		
		ОткрытьФорму("Обработка.Тестирование.Форма", Новый Структура("Тест", Объект.Тест));
	Иначе
		ЭтотОбъект.ЗаблокироватьДанныеФормыДляРедактирования();
	КонецЕсли;
КонецПроцедуры
