
Процедура СправочникОбъектПередЗаписьюПередЗаписью(Источник, Отказ) Экспорт
	Мета = Источник.Метаданные();
	Если Мета.ДлинаКода > 0 И Источник.Метаданные().ТипКода = Метаданные.СвойстваОбъектов.ТипКодаСправочника.Строка Тогда
		Источник.Код = СокрЛП(Источник.Код);
	КонецЕсли;
	Если Мета.ДлинаНаименования > 0 Тогда
		Источник.Наименование = СокрЛП(Источник.Наименование);
	КонецЕсли;
КонецПроцедуры
